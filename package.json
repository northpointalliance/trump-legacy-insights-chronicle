{
  "name": "the-presidency-ledger",
  "private": true,
  "type": "module",
  "description": "The Presidency Ledger: static reference site at https://thepresidencyledger.com",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npx --yes serve dist -p 8788",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=trump-legacy-insights-chronicle --branch=main --commit-dirty=true"
  }
}
